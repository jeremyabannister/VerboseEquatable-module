import XCTest
@testable import VerboseEquatable_module

final class VerboseEquatable_moduleTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(VerboseEquatable_module().text, "Hello, World!")
    }
}
